# Secrets — never commit real credentials
.env
.env.local
.env.*.local

# Dependencies & artifacts
node_modules/
coverage/
*.log

# Local runtime persistence (JSONL stores at the repo root). Scoped to the root
# so it never swallows source directories like src/data/ or test/data/.
/data/
